{
  "id": "textilelib",
  "name": "TextileLib",
  "description": "Adding more events into the game",
  "version": "${version}",
  "side": "universal",
  "requires": {
    "fabric": "*"
  },
  "mixins": {
    "client": "mixins.textilelib.client.json",
    "common": "mixins.textilelib.common.json"
  }
}
